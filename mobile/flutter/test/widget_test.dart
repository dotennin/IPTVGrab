import 'package:flutter_test/flutter_test.dart';

void main() {
  // Placeholder until device/FFI-aware widget tests are added.
  testWidgets(
    'placeholder',
    (tester) async {},
    skip: true,
  );
}
